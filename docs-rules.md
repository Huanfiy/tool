# docs-rules — 文档记录与清理规则

| 项 | 内容 |
| ---- | ---- |
| 定位 | AI 辅助开发长期迭代中的信息治理规则：明确记录什么、记在哪、何时清理、怎么清理 |
| 不覆盖 | git 历史管理（历史永不改写） |

## 1. 原则

- 工作区只保留两类信息：**当前事实**（现行设计、现行协议、现行流程）与**长期记录**（实测数据、优化结论等不可再生信息）。其余归 git 历史。
- 同一事实只允许一处正文，其余位置以链接指向；历史版本由 git 承担，文档不承担版本职能。
- AI 会话产出信息的速度远高于人工，默认动作是「不记录」；落档需过 §3 的记录门槛。
- 文档与代码冲突时，以代码为准修正文档；冲突表述即清理对象。

## 2. 记录框架

| 层 | 位置 | 装什么 | 生命周期 |
| ---- | ---- | ---- | ---- |
| 对外说明 | `README.md` | 站点定位、功能清单、技术栈、目录结构，以及本地预览 / 测试 / 部署的使用方式 | 随功能变更同轮更新 |
| 协作事实速查 | `AGENTS.md` | AI 协作所需的仓库现状：项目索引、必须遵守的约束、开发与验证入口、提交约定 | 随仓库现状同轮更新 |
| 拍板设计 | `docs/design/`；工作室要素约束沿用既有 `design/workbench.md` | 设计系统、博客发布流程与封面、发布产物与部署边界、工作室布局与交互约束等已拍板的架构与设计说明 | 就地更新，始终等于当前事实 |
| 长期备忘（验证记录） | `docs/testing/` | 浏览器 / 真机验证证据：环境、基线提交、实测数据、优化结论与待验收项等不可再生信息 | 长期保留；对应实现变更后失实即修正或清理 |
| 目录操作说明 | `interests/*/README.md` | 与页面数据强耦合的目录用法：素材放置位置、`index.json` 字段约定 | 随目录内约定同轮更新 |
| 待办登记 | `docs/todo/` | 已评审但暂缓执行的改进项，一项一文件（背景、现状、建议动作、缓办原因） | 执行完成即删除对应文件 |
| 变更记录 | git log | 变更主题与验证结论（一笔一主题） | 永久，不改写 |

## 3. 记录门槛

全部满足才落档，否则不记录：

1. 面向未来读者（下一轮会话、他机、他人），而非面向当前对话；
2. 无法从代码、配置、git 历史直接推导；
3. 有明确归属层（§2），且该层尚无同一事实——已有则更新原文，不另起新文。

## 4. 不记录清单

- 一次性 implementation plan：实现合入后，过程信息由 git log 承载，plan 本身不入库。
- AI 工具目录（`.claude/`、`.cursor/`、`.agents/`）：属个人开发环境配置，一律不入库，统一由 `.gitignore` 隔离。
- 未决探索草稿（方案对比、推导过程）：留在已隔离位置（AI 工具目录、`tmp/` 等），拍板后按 §3 门槛落档结论。
- 可推导事实：配置内容不复述进文档（引用文件路径即可），变更历史不复述（git log 即记录）。
- 过程性对话、试错细节、被否弃的中间方案。
- 一次性中间产物（探针脚本、临时日志、生成物）：放 `tmp/` 或仓库外，不入库。

## 5. 清理准则

**触发点**（事件驱动，不设固定周期）：

- 功能合入或模块删除，导致既有文档失实；
- 设计变更拍板，旧设计描述失去「当前事实」属性；
- 发现文档表述与代码现状冲突（以代码为准修正，冲突表述即清理对象）。

**判据三问**（全「否」即清理）：

1. 它描述的还是当前事实吗？
2. 它支撑的事项还未决吗？
3. 它有 git 历史之外、不可再生的价值吗？

**清理方式**：

- 文件级：移出工作区；所有引用处改为「已移出工作区，git 历史 `<引入提交 hash>` 可查」；不留墓碑文件。
- 章节级：正文清空，保留章节号与一行墓碑说明（保持编号稳定，避免既有引用漂移）。
- 清理独立成笔提交，提交说明写明清理对象与理由；格式遵循 [AGENTS.md](AGENTS.md) 的提交约定，纯文档清理写作 `📝 docs(<scope>): <subject>`。
